% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_valuation_excel.R
\name{read_valuation_excel}
\alias{read_valuation_excel}
\title{Read and validate a valuation Excel file}
\usage{
read_valuation_excel(path, sheet = 1)
}
\arguments{
\item{path}{A length-one character string giving the path to the \code{.xlsx} file.}

\item{sheet}{A length-one character string or positive integer specifying
which sheet to read. Defaults to \code{1} (the first sheet).}
}
\value{
A tibble with one row per company. Column names are standardized
using \code{\link[=make.names]{make.names()}} so they are safe to use in R expressions.
}
\description{
Reads a standardized comparable company analysis Excel file and returns a
clean tibble. The function validates that all required columns are present
and that key numeric columns contain non-negative values, ensuring the data
is ready for downstream analysis.
}
\examples{
tmp <- tempfile(fileext = ".xlsx")
writexl::write_xlsx(valuation_data, tmp)
df <- read_valuation_excel(tmp)
head(df)
}
